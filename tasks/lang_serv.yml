---

- name: PythonLSP - pyright
  community.general.npm:
    name: pyright
    global: true
  become: true

- name: luaLSP - Sumneko - Create directory
  file:
    path: "{{ home_dir}}/.opt/lua-language-server/"
    state: directory
    mode: 0755

- name: luaLSP - Sumneko - Download
  get_url:
    url: "https://github.com/sumneko/lua-language-server/releases/download/{{lua_lsp_version}}/lua-language-server-{{lua_lsp_version}}-linux-x64.tar.gz"
    dest: "{{ home_dir }}/Downloads/lua-language-server.tar.gz"

- name: luaLSP - Sumneko - Install
  unarchive:
    src: "{{ home_dir }}/Downloads/lua-language-server.tar.gz"
    dest: "{{ home_dir }}/.opt/lua-language-server"
    mode: 0755
  become: true

- name: Gopls - Install
  shell: go install golang.org/x/tools/gopls@latest

- name: Move directory
  copy:
    src: "{{ home_dir }}/go"
    dest: "{{ home_dir }}/.go"
