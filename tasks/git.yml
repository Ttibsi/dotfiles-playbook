---

- name: install
  apt:
    name: git
    only_upgrade: true
  become: true

- name: Move git config
  copy:
    src: templates/git-config/gitconfig
    dest: "{{ home_dir }}/.gitconfig"

# Tests
# cat $HOME/.gitconfig
